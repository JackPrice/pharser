engines:
    duplication:
        enabled: true
        config:
            languages:
              - php
    fixme:
        enabled: true
    phpcodesniffer:
        enabled: true
    phpmd:
        enabled: true


exclude_paths:
  - tests/**/*

ratings:
    paths:
      - "**.php"
